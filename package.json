{
  "name": "@benmar/lsp-mcp",
  "version": "0.1.0",
  "description": "MCP server exposing LSP refactoring operations as Claude Code tools",
  "license": "MIT",
  "author": "Benjamin Schools",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/bin.js",
  "bin": {
    "lsp-mcp": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "prepare": "tsc",
    "prepublishOnly": "pnpm run build && pnpm run test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
